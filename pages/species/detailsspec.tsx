/* import { describe, it, expect } from 'vitest';
import { Details } from './details';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';

describe('check what is going on in card details', () => {
  it('card details has text', () => {
    render(
      <Details
        name="lalala"
        classification="lalala"
        designation="lalala"
        average_height="lalala"
        hair_colors="lalala"
        skin_colors="lalala"
        eye_colors="lalala"
        average_lifespan="lalala"
        language="lalala"
      />
    );
    const element = screen.getByRole('heading', { name: /Name:/i });
    expect(element).toBeInTheDocument();
  });
}); */
